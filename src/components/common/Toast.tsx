import React from 'react';

// Floating popup notification banner disabled as per user request
// Notifications are logged cleanly in the dedicated Notifications view instead.
export const Toast: React.FC = () => {
  return null;
};

